#define HOST_NAME "NAME"          // Name for device in local network
#define WIFI_SSID "SSID"          // Name of local wifi network
#define WIFI_PASSWORD "PASSWORD"  // Password to local wifi network

#define SERVER_IP 1, 1, 1, 1  // IP of server with mqtt broker
#define SERVER_PORT 1111      // port of mqtt broker
#define STATUS_TOPIC "TOPIC"  // topic for device status (connected, reconnected, disconnected)
#define RELAY_TOPIC "TOPIC"   // topic for controlling the relay (0, 1)

#define OTA_PASSWORD "PASSWORD"  // Arduino OTA authentication password